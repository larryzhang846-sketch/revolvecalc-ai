# Optional: enable OpenAI vision for real image-aware captions.
# Without this, AURA uses a curated local generator (still polished for demos).
OPENAI_API_KEY=

# Optional override (default: gpt-4o-mini)
# OPENAI_VISION_MODEL=gpt-4o-mini
